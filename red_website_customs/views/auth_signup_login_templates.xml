<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <!--AUTH SIGNUP-->
        <template id="auth_signup_fields" inherit_id="auth_signup.fields" name="Auth Signup/ResetPassword form fields">
            <div class="mb-3 field-name" position="attributes">
                <attribute name="style">display:none;</attribute>
            </div>
            <div class="mb-3 field-name" position="after">
                <div class="mb-3 field-firstname">
                    <label for="first_name">First Name</label>
                    <input type="text" name="first_name" t-att-value="first_name" id="first_name" class="form-control form-control-sm" placeholder="e.g. John"
                        required="required" t-att-readonly="'readonly' if only_passwords else None"
                        t-att-autofocus="'autofocus' if login and not only_passwords else None" pattern="^\S+$" title="No valid White Spaces"/>
                </div>
                <div class="mb-3 field-lastname">
                    <label for="last_name">Last Name</label>
                    <input type="text" name="last_name" t-att-value="last_name" id="last_name" class="form-control form-control-sm" placeholder="e.g. Doe"
                        required="required" t-att-readonly="'readonly' if only_passwords else None" pattern="^\S+$" title="No valid White Spaces"/>
                </div>
                <div class="mb-3 field-surname">
                    <label for="surname">Surname</label>
                    <input type="text" name="surname" t-att-value="surname" id="surname" class="form-control form-control-sm" placeholder="e.g. Doe"
                        required="required" t-att-readonly="'readonly' if only_passwords else None" pattern="^\S+$" title="No valid White Spaces"/>
                </div>
            </div>
            <div class="mb-3 field-name" position="before">
                <div class="mb-3 field-login" position="move"/>
            </div>
            <input name="login" position="attributes">
                <attribute name="pattern">^\S+$</attribute>
                <attribute name="title">No valid White Spaces</attribute>
            </input>
            <input name="name" position="attributes">
                <attribute name="required"/>
            </input>
        </template>
</odoo>
